name: Validate skills

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: lumea-labs/polpo
          path: .contract/polpo
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/validate-skills.mjs
        env:
          POLPO_OSS_ROOT: ${{ github.workspace }}/.contract/polpo
