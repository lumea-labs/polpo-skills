# Contract Version

Verified on 2026-08-24 against Polpo OSS `0.15.109`, project layout version `2`, and the current
`AgentConfig`, model router, execution router, chat interaction, tool policy, and sandbox types.

For another version, inspect the installed schemas before authoring fields. Do not preserve
legacy aggregate agent files or `provider:model` identifiers merely because an old local skill
mentions them.
